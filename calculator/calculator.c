#include <stdio.h>
#include <math.h>

int main(void)
{
    int x = x;
    int y = y;
    printf("Enter value of x: ");
    scanf("%d", &x);
    printf("Enter the value of y: ");
    scanf("%d", &y);
    printf("the result of the sum is: %i\n", x + y);
}